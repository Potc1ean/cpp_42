#include <iostream>
#include <string>

int convertor()
{
    
}

int main()
{

    return 0;
}


/*
Donc en gros on a une string qui contient le chamin du fichier 
ce fichier on doit l'ouvrir avec qq chose 
une fois ouvert on doit stock mots par mots la phrases
on mats dans une string de retour un a un les mots par mots la phtases donnees

avant de donnees un mot, il faut verifier si il correspond au premier de ce qu'il
faut remplacer. bref t'as capte

et on est sense finir avec le return a la fin
*/